Enter your stuff and click 'set', if you want a random adventure click 'just THROW me' and then click set. Saves,loads and exports are all handles through he storage section of the script interface.
